name: "hello word workflow"

on:
    push:
      branches:
          - master
          - production

jobs:
    hello_word:
      runs-on: ubuntu_latest
      steps:
          - name: "print hello world"
            run: echo "hello world"